name: "DCO Check"
on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  dco:
    name: DCO Signed-off-by
    runs-on: ubuntu-latest
    steps:
      - name: Check DCO
        uses: tisonkun/actions-dco@v1.3
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
